import { Snack } from "snack-sdk";

export const getPreviewUrl = async (options) => {
  // const snack = new Snack(options);

  // const code = await snack.saveAsync();

  // return `https://snack.expo.dev/${code.id}`;
  console.log("Snack", Snack);
};
